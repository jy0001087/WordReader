<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.leathersheer.tools.SpiderUnit.LianjiaUnits.LianjiaMapper">
    <select id="conntest" resultType="String">
    select 'conn is ok!' as txt;
    </select>

    <insert id ="insertHouseinfo" parameterType="java.util.LinkedList" >
                 insert into HouseInfo
                  (Address, Area, Gdlocation, Floor,Price,HouseType,Oriented,house_code,update_timestamp,houseurl,house_status)
                  values
                      (
                      #{address}, 
                      #{area},
                      #{gdlocation},
                      #{floor},
                      #{price},
                      #{housetype},
                      #{oriented},
                      #{house_code},
                      #{update_timestamp},
                      #{houseurl},
                      #{house_status}
                      )
     </insert >
</mapper>