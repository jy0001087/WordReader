<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.leathersheer.tools.SpiderUnit.ShellRental.ShellRentalMapper">

    <insert id="insertAll" parameterType="com.leathersheer.tools.SpiderUnit.ShellRental.ShellHouseBean">
        Insert into shellrentalhouseinfo(houseid,address,price,url,landmark,proportion,housetype)
        values (#{houseid},#{address},#{price},#{url},#{landmark},#{proportion},#{housetype})
        on conflict do nothing
    </insert>

</mapper>